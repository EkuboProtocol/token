{
  "name": "ekubo-token",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "solhint --disc --noPoster --max-warnings 0 'src/**/*.sol' 'script/**/*.sol' 'test/**/*.sol'"
  },
  "devDependencies": {
    "solhint": "6.2.4"
  }
}
